# CLAUDE.md

Claude Code가 이 프로젝트에서 작업할 때 따르는 지침입니다.

## 프로젝트

헤드헌팅 후보자 검색·제안 업무 자동화 (크몽 외주, MVP 단계).
상세 배경·합의사항·미확정 항목은 `PROJECT_CONTEXT.md`를 먼저 읽을 것.
클라이언트 원본 요청 대화 파일은 납품 저장소에 포함하지 않습니다. 요구사항은 `PROJECT_CONTEXT.md`와 `PRD.md`를 기준으로 관리합니다.

## 작업 시작 시 반드시

1. `PROJECT_CONTEXT.md` 전체를 읽고 현재 단계를 파악한다.
2. "7. 미확정 항목"과 "9. 다음 단계"를 확인해 지금 무엇을 할 차례인지 정한다.
3. 새로 확정된 사항이 생기면 `PROJECT_CONTEXT.md`를 직접 갱신한다 (체크박스, 결정사항).

## 진행 원칙

- **현장 검증 우선**: 리멤버 DOM 구조와 VPN 환경 제약이 확인되기 전까지
  셀렉터·네트워크 가정을 코드에 하드코딩하지 않는다. 미확정 부분은 명시한다.
- **안정성 > 속도**: 리멤버 계정은 1개뿐이며 정지 시 클라이언트 업무가 마비된다.
  크롤링은 보수적 속도(요청 간 지연, 인간적 패턴)로 설계한다.
- **발송은 반자동**: 매칭 후 후보자·근거를 사람이 보고 승인할 때만 발송한다.
  사람 확인 없는 대량 자동 발송 코드는 작성하지 않는다.
- **개인정보 최소화**: 후보자 이력서를 OpenAI API로 보낼 때 필요한 텍스트만
  전송한다. 로컬에 불필요하게 원문을 저장하지 않는다.

## 기술 방향 (잠정)

- 언어/스택: Python. 브라우저 자동화는 Playwright (현장 검증 후 확정).
- UI: 로컬 웹 (Python 백엔드 + localhost 브라우저 접속).
- LLM: OpenAI API (조건식 생성 + 매칭 분석). API 키 주체는 미확정.
- 실행 환경: VPN이 설치된 클라이언트 회사 PC 내부에 직접 설치.

## 코드 작성 규칙

- 비밀번호·API 키 등 민감정보는 코드에 하드코딩하지 않고 `.env`로 분리한다.
- `.env`, 로그인 세션, 후보자 데이터는 `.gitignore`에 포함한다.
- 한국어 주석/로그 사용 (클라이언트 인수인계 고려).
