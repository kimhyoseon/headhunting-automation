# 클라이언트 PC 실연동 개발 PRD

## 1. 목적

클라이언트 회사 PC에서 현재 Git 저장소를 내려받아 실행한 뒤, 리멤버 채용솔루션에 로그인된 실제 화면을 대상으로 후보자 이력서 크롤링과 제안 발송 버튼 클릭까지 이어서 개발한다.

이 문서는 다음 개발자가 Codex 또는 로컬 개발 환경에서 바로 이어받기 위한 작업지시서다.

## 2. 현재 구현 상태

현재 저장소는 로컬 실행형 FastAPI 앱이다.

- `run_app.bat` 또는 `python run_app.py`로 실행 가능
- 앱 주소: `http://127.0.0.1:8000`
- 실행 시 왼쪽 창에는 로컬 앱, 오른쪽 창에는 리멤버 채용솔루션이 열리도록 준비됨
- 리멤버 기본 URL: `https://career.rememberapp.co.kr/`
- OpenAI API 기반 후보자 매칭 흐름 구현됨
- 더미 후보자 기반 1~4단계 UI 흐름 구현됨
- `BrowserRememberAdapter`는 CDP 연결과 탭 점검까지만 구현됨
- 실제 리멤버 후보자 목록 크롤링, 상세 레이어 이력서 추출, 발송 버튼 클릭은 미구현

현재 `CRAWLER_MODE=browser`로 실행을 시작하면 실제 연동이 완성되지 않았기 때문에 501 오류가 발생한다. 실연동 개발의 핵심은 `MockRememberAdapter`를 실제 브라우저 제어 어댑터로 교체하는 것이다.

## 3. 클라이언트 PC 준비 절차

1. 클라이언트 회사 PC에서 Git 저장소를 다운로드한다.
2. Python 3.11 이상이 설치되어 있는지 확인한다.
3. 저장소 루트에서 `run_app.bat`을 실행한다.
4. 앱이 뜨면 설정 화면에서 OpenAI API 키를 저장한다.
5. 오른쪽 리멤버 창에서 사용자가 직접 로그인한다.
6. 리멤버에서 후보자 검색 조건을 직접 입력하고, 1차 매칭이 끝난 후보자 목록 화면까지 이동한다.
7. 이 상태에서 왼쪽 앱의 크롤링 테스트 또는 실행 버튼을 눌러 실제 DOM 분석과 개발을 시작한다.

주의:

- `.env`, `browser_profile/`, 로그인 세션, 후보자 원문 데이터는 Git에 올리지 않는다.
- 후보자 이력서 원문은 파일/DB에 저장하지 않는다.
- 로그에는 후보자 원문 전체를 남기지 않는다.

## 4. 목표 사용자 흐름

### 4.1 사용자가 직접 준비하는 부분

사용자는 리멤버 오른쪽 창에서 다음 작업을 직접 완료한다.

1. 리멤버 채용솔루션 로그인
2. 후보자 검색 조건 입력
3. 1차 매칭 또는 필터링 완료
4. 자동화 대상 후보자 목록 화면 진입

이후 앱은 현재 열린 리멤버 후보자 목록 화면을 기준으로 동작한다.

### 4.2 앱이 자동화할 부분

1. 후보자 목록 화면에서 후보자 카드를 순회한다.
2. 후보자 카드를 클릭한다.
3. 오른쪽 상세 레이어 또는 상세 영역에서 이력서 정보를 추출한다.
4. 추출한 이력서 텍스트와 JD를 OpenAI API로 매칭한다.
5. `API 분석 상한` 또는 실제 후보자 목록 종료 중 먼저 도달한 조건까지 반복한다.
6. 2단계 분석 완료 후 3단계 발송 화면에 매칭 통과 후보자를 표시한다.
7. 사용자가 체크한 후보자만 발송 대상으로 삼는다.
8. 3단계 실행 시 후보자를 다시 클릭한다.
9. 오른쪽 상세 레이어에서 리멤버 기본 제안/발송 버튼을 클릭한다.
10. 모든 선택 후보자 발송이 끝나면 4단계 결과 화면에 요약한다.

## 5. 구현 범위

### 5.1 포함

- Playwright CDP로 이미 열린 리멤버 브라우저 창에 연결
- 현재 리멤버 후보자 목록 화면 탐지
- 후보자 카드 셀렉터 분석 및 순회
- 긴 스크롤 목록 처리
- 페이징 또는 무한 스크롤 처리
- 중복 후보자 스킵
- 후보자 상세 레이어 열기
- 이력서/프로필 텍스트 추출
- OpenAI 매칭 호출
- 매칭 결과를 기존 2~4단계 UI에 표시
- 사용자가 선택한 후보자만 실제 발송
- 후보자 간 랜덤 지연
- 로그아웃, 차단/캡차, 연속 발송 실패 감지

### 5.2 제외

- 리멤버 로그인 자동화
- 리멤버 검색 조건 자동 입력
- Boolean 검색식 자동 입력
- 후보자 CRM 저장
- 후보자 이력서 원문 파일 저장
- 사람 확인 없는 완전 자동 대량 발송

## 6. 핵심 구현 지시

### 6.1 어댑터 연결

대상 파일:

- `app/main.py`
- `app/services/runner.py`
- `app/services/remember_browser.py`
- `app/services/remember.py`

작업:

- `CRAWLER_MODE=mock`이면 현재처럼 `MockRememberAdapter`를 사용한다.
- `CRAWLER_MODE=browser`이면 `BrowserRememberAdapter`를 사용한다.
- `RunManager`의 타입 힌트는 특정 mock 클래스가 아니라 공통 인터페이스 형태로 정리한다.
- `BrowserRememberAdapter.search()`, `open_candidate()`, `send_proposal()`을 실제 구현한다.

### 6.2 후보자 목록 크롤링

`search(limit)`는 현재 열린 리멤버 후보자 목록 화면을 기준으로 후보자를 수집한다.

필수 동작:

- 현재 리멤버 탭 찾기
- 로그인 만료 화면인지 확인
- 후보자 목록 카드 셀렉터 찾기
- 후보자별 안정적인 식별자 생성
- `limit`명 또는 목록 끝까지 후보자를 수집
- 스크롤을 내려 추가 후보자 로드
- 페이징 버튼이 있으면 다음 페이지 이동
- 이미 수집한 후보자는 중복 처리하지 않음

후보자 모델 매핑:

- `id`: 리멤버 내부 후보자 ID 또는 URL/이름/회사 조합 기반 안정 ID
- `name`: 후보자 이름
- `company`: 현재 회사
- `role`: 현재 직무
- `experience`: 경력
- `location`: 지역
- `skills`: 목록 또는 상세에서 얻은 기술 키워드
- `resume_text`: 상세 레이어에서 추출한 이력서 본문

목록 단계에서 이력서 원문을 얻을 수 없으면, `search()`는 후보자 카드 참조 정보만 만들고 `open_candidate()`에서 본문을 채운다.

### 6.3 후보자 상세 이력서 추출

`open_candidate(candidate)`는 후보자 카드를 다시 찾거나 현재 후보자 카드에서 상세 레이어를 연다.

필수 동작:

- 후보자 카드 클릭
- 오른쪽 상세 레이어가 뜰 때까지 대기
- 상세 레이어 내 이력서/경력/학력/스킬/소개 영역 텍스트 추출
- 불필요한 버튼/메뉴/광고 텍스트 제거
- 추출 실패 시 해당 후보자를 실패 처리하고 다음 후보자로 이동

추출 텍스트는 OpenAI 매칭에 충분해야 하지만, 개인정보 최소화 원칙에 따라 화면 전체 HTML이나 불필요한 원문을 저장하지 않는다.

### 6.4 매칭 반복 조건

반복은 다음 중 먼저 도달하면 종료한다.

- 앱에서 설정한 `max_candidate_count`
- 리멤버 후보자 목록의 마지막
- 사용자가 중단 버튼 클릭
- 로그아웃/차단/캡차 감지
- 반복 불가능한 DOM 변경 또는 치명적 오류

후보자 간 동작에는 `.env`의 `MIN_DELAY_SECONDS`, `MAX_DELAY_SECONDS`를 사용한다. 실제 운영 기본값은 30~90초로 조정한다.

### 6.5 발송 단계

발송 정책은 “선택 후 자동”이다.

2단계 분석 후 3단계 화면에서 사용자가 후보자를 체크한다. `send_selected()` 호출 시 `BrowserRememberAdapter.send_proposal(candidate)`가 실제 리멤버 화면에서 다음을 수행한다.

1. 후보자 목록 또는 현재 페이지에서 대상 후보자를 다시 찾는다.
2. 후보자 상세 레이어를 연다.
3. 리멤버 기본 제안/발송 버튼을 찾는다.
4. 버튼 클릭 후 성공/실패 UI를 확인한다.
5. 결과를 `sent`, `failed`, `skipped` 중 하나로 반영한다.

발송 중 다음 상황은 실패 사유로 분류한다.

- 이미 제안한 후보자
- 버튼 비활성화
- 권한 없음
- 팝업/모달 확인 필요
- 네트워크 타임아웃
- 리멤버 UI 변경으로 버튼 탐지 실패

연속 발송 실패가 3건이면 즉시 자동 정지한다.

## 7. 실제 DOM 분석 지시

실제 리멤버 화면 구조는 클라이언트 PC에서 원격접속으로 확인한다.

분석해야 할 화면:

1. 후보자 목록 화면
2. 후보자 카드
3. 오른쪽 후보자 상세 레이어
4. 이력서/경력 상세 영역
5. 제안/발송 버튼
6. 발송 성공 메시지
7. 이미 발송됨/발송 불가 메시지
8. 로그아웃 화면
9. 캡차 또는 비정상 접근 차단 화면
10. 페이징 또는 무한 스크롤 로딩 영역

개발자는 먼저 앱의 `크롤링 테스트`를 실행해 CDP 연결, 리멤버 탭 탐지, 현재 화면 텍스트 수집이 되는지 확인한다. 그 다음 Playwright inspector 또는 브라우저 개발자도구로 안정적인 셀렉터를 확정한다.

셀렉터 우선순위:

1. 의미 있는 `data-*` 속성
2. `aria-label`, role, button text
3. URL 또는 내부 후보자 ID
4. 안정적인 텍스트 조합
5. CSS 클래스명은 마지막 수단

## 8. 안전장치

필수 안전장치:

- 로그인 만료 감지 시 즉시 정지
- 캡차/차단/비정상 접근 문구 감지 시 즉시 정지
- 연속 발송 실패 3건 시 즉시 정지
- 후보자 간 랜덤 지연
- 실행 중 사용자의 일시정지/중단 가능
- 발송은 사용자가 체크한 후보자만 수행

운영 권장값:

```env
MIN_DELAY_SECONDS=30
MAX_DELAY_SECONDS=90
CRAWLER_MODE=browser
REMEMBER_URL=https://career.rememberapp.co.kr/
REMEMBER_CDP_URL=http://127.0.0.1:9222
```

## 9. 완료 기준

다음 항목을 모두 만족하면 클라이언트 PC 실연동 MVP 완료로 본다.

- `run_app.bat` 실행 후 앱과 리멤버 창이 좌우로 열림
- 리멤버 로그인 후 후보자 목록 화면에서 CDP 연결 성공
- 후보자 5명 이상을 실제 목록에서 순회 가능
- 후보자 상세 레이어에서 이력서 텍스트 추출 가능
- 추출 텍스트로 OpenAI 매칭 점수 표시 가능
- 긴 스크롤 또는 페이징 목록에서 `max_candidate_count`까지 반복 가능
- 3단계에서 사용자가 선택한 후보자만 실제 발송 가능
- 발송 성공/실패 사유가 4단계 결과 화면에 표시됨
- 로그아웃/차단/캡차/연속 실패 3건 감지 시 자동 정지
- 후보자 원문 데이터가 Git, 로그, 파일, DB에 저장되지 않음

## 10. 클라이언트 PC에서 Codex에게 전달할 안내문

아래 문구를 그대로 Codex에게 전달하면 된다.

```text
현재 저장소는 헤드헌팅 후보자 제안 자동화 MVP입니다.

클라이언트 PC에서 Git 저장소를 내려받았고, `run_app.bat`으로 앱을 실행할 예정입니다.
앱은 왼쪽 로컬 UI와 오른쪽 리멤버 채용솔루션 창을 함께 엽니다.
오른쪽 리멤버 주소는 `https://career.rememberapp.co.kr/`입니다.

사용자가 리멤버에 직접 로그인하고, 후보자 검색 및 1차 매칭 후보자 목록 화면까지 이동한 상태에서 이어서 개발해주세요.

현재 구현된 것:
- 로컬 FastAPI 앱
- 1~4단계 UI
- OpenAI 후보자 매칭
- 더미 후보자 기반 전체 흐름
- CDP 브라우저 연결 테스트

미구현된 것:
- 실제 리멤버 후보자 목록 크롤링
- 후보자 클릭 후 오른쪽 상세 레이어 이력서 추출
- 스크롤/페이징 처리
- 실제 리멤버 기본 제안/발송 버튼 클릭
- 실제 DOM 기반 로그아웃/차단/캡차 감지

개발 목표:
1. `CRAWLER_MODE=browser`에서 `BrowserRememberAdapter`가 실제 리멤버 탭에 연결되게 해주세요.
2. 후보자 목록 화면에서 후보자를 순회하면서 카드 클릭 → 오른쪽 레이어 이력서 텍스트 추출 → OpenAI API 매칭을 반복해주세요.
3. 반복은 앱의 API 분석 상한 또는 후보자 목록 끝 중 먼저 도달하는 조건까지 진행해주세요.
4. 3단계에서는 사용자가 체크한 후보자만 다시 클릭하고, 오른쪽 레이어의 리멤버 기본 제안/발송 버튼을 눌러주세요.
5. 발송 결과와 실패 사유를 4단계 결과 화면에 반영해주세요.
6. 개인정보 원문은 파일/DB/로그에 저장하지 말고 메모리에서만 처리해주세요.
7. 로그인 만료, 캡차/차단 화면, 연속 발송 실패 3건은 즉시 자동 정지해주세요.

먼저 `CLIENT_PC_IMPLEMENTATION_PRD.md`, `BROWSER_WORKSPACE.md`, `app/services/remember_browser.py`, `app/services/runner.py`, `app/main.py`를 읽고 현재 구조를 파악한 뒤 진행해주세요.
```
