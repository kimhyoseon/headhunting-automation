# GitHub 이동 후 이어 구현할 항목

현재 구현은 제품 흐름 검증용 MVP입니다. 리멤버 접속 권한이 있는 회사 PC에서 아래 작업을 이어 진행해야 실제 자동화가 완성됩니다.

## 1. 리멤버 실제 크롤링

현재 상태:
- `app/services/remember.py`의 `MockRememberAdapter`가 더미 후보자 30명을 반환합니다.
- 테스트 비용은 시작 화면의 `크롤링 · API 분석` 상한으로 조절합니다.
- 더미 후보자 데이터는 `app/mock_data.py`에 보관되어 있습니다.
- 검색, 상세 열람, 이력서 추출은 실제 브라우저를 열지 않습니다.

이어 할 일:
- `PlaywrightRememberAdapter`를 새로 추가합니다.
- 리멤버 로그인 상태를 수동으로 만든 뒤 세션을 재사용하는 방식을 우선 검토합니다.
- 검색창, 검색 결과 카드, 상세 페이지, 이력서 영역 셀렉터를 현장 PC에서 확인합니다.
- 후보자 간 지연 시간을 `.env` 기준 30~90초로 조정합니다.
- 로그아웃/세션 만료, 캡차/차단 페이지 감지 셀렉터를 추가합니다.

권장 검증 순서:
1. `playwright install chromium`
2. 리멤버 로그인 페이지 진입 가능 여부 확인
3. 검색어 입력 및 결과 리스트 DOM 캡처
4. 후보자 상세 페이지 DOM 캡처
5. 이력서 텍스트 추출 정확도 확인

## 2. 실제 제안 발송

현재 상태:
- `send_proposal()`은 더미 성공/실패 결과만 반환합니다.
- 테스트모드 ON/OFF의 UI 흐름은 연결되어 있지만 실제 메시지를 보내지 않습니다.

이어 할 일:
- 후보자 상세의 제안/메시지 버튼 셀렉터를 확인합니다.
- 리멤버 후보자 상세 화면의 제안 버튼 셀렉터를 확인합니다. 제안 문구는 리멤버 사이트의 기본 발송 흐름을 사용합니다.
- 발송 성공 토스트/상태 변화 셀렉터를 확인합니다.
- 동일 후보자 재접촉 차단, 전송 실패, UI 변경을 실패 사유로 분류합니다.
- 연속 발송 실패 3건 자동 정지 로직은 기존 `RunManager` 흐름에 연결합니다.

## 3. 현장 검증

반드시 확인할 항목:
- VPN 환경에서 `https://api.openai.com/v1/models` 호출 가능 여부
- 회사 PC에서 Playwright Chromium 실행 가능 여부
- 리멤버가 자동화 브라우저를 차단하지 않는지 여부
- 후보자 이력서 텍스트를 OpenAI API로 보내는 것에 대한 클라이언트 동의

## 4. 코드 연결 지점

- `app/services/remember.py`
  - 현재 `MockRememberAdapter`를 실제 `PlaywrightRememberAdapter`로 교체할 대상입니다.
- `app/services/runner.py`
  - `search()`, `open_candidate()`, `send_proposal()` 호출부는 이미 어댑터 방식으로 분리되어 있습니다.
- `app/main.py`
  - `remember = MockRememberAdapter()`를 실제 어댑터로 바꾸면 됩니다.

## 5. 주의사항

- 실제 후보자 이력서 원문을 파일/로그/DB에 저장하지 마세요.
- `.env`, 브라우저 세션, 쿠키, 실제 후보자 데이터는 GitHub에 올리지 마세요.
- 첫 실운영 1~2주는 테스트모드 ON으로 매칭 품질과 발송 안전성을 확인하는 것을 권장합니다.
- 실제 발송 전 지연 시간은 30~90초로 되돌리세요.
