# PRD — 헤드헌팅 후보자 제안 자동화 (MVP)

| 항목 | 값 |
| --- | --- |
| 프로젝트 | 헤드헌팅 후보자 검색·매칭·제안 자동화 |
| 의뢰 경로 | 크몽 gig/756316 (주문번호 7361793, 99,000원) |
| 작성 기준일 | 2026-05-26 |
| 작성 기반 | `PROJECT_CONTEXT.md` (확정 6-1 ~ 6-11) + 초기 요구사항 정리 |
| 단계 | PRD 확정안. 이후 현장 검증 → MVP 구현 진입 |

---

## 1. 목적과 범위

### 1.1 목적
헤드헌터 1인이 반복 수행하는 **검색조건 생성 → 리멤버 검색 → 이력서 확인 →
매칭도 판단 → 제안 발송** 사이클을 자동화하여 일일 처리량을 끌어올린다.

### 1.2 범위 (MVP에 포함)
- JD(채용공고/회사정보) 텍스트 입력
- GPT 기반 Boolean 조건식 생성
- 리멤버 자동 검색·후보자 순회
- 이력서 텍스트 추출
- 항목별 가중평균 기반 매칭 점수 산출
- 임계값(슬라이더) 통과자 자동 또는 일괄승인 발송
- 실시간 진행 표시 및 이상 감지 자동 정지

### 1.3 범위 외 (MVP 제외, v2 후보)
- JD/발송 이력 저장 및 재실행
- **목표 미달 시 자동 조건 완화·재실행** (의뢰서 원본 요구 중 의도적 제외, 6-8)
- 후보자 CRM/태그/메모
- 다중 사용자, 권한 관리
- 발송 거부율 기반 자동 일시정지
- 모바일/원격 알림

### 1.4 사용자
- **클라이언트 1인** (특정 회사 VPN PC에서 단독 사용)
- 헤드헌팅 실무자, 비개발자
- 회사 PC에 개인 PC로 원격 데스크톱 접속하여 사용

---

## 2. 사용 환경

| 항목 | 내용 |
| --- | --- |
| 실행 PC | 클라이언트 회사 PC (Windows) |
| 네트워크 | BLUEMAX ESP SSL VPN 환경 |
| 접속 방식 | 클라이언트 개인 PC → Google Chrome 원격 데스크톱 → 회사 PC |
| 개발자 접속 | 동일하게 Chrome 원격 데스크톱으로 회사 PC 직접 접속하여 개발·설치 |
| 리멤버 제약 | 특정 회사 PC에서만 로그인 가능. 계정 1개 |

---

## 3. 사용자 시나리오 (To-Be)

### 3.1 표준 시나리오 (테스트모드 OFF, 기본값)
1. 클라이언트가 브라우저로 `http://127.0.0.1:<port>` 접속
2. JD 텍스트 붙여넣기, 통과 기준 슬라이더(기본 90) 조정
3. **시작** 클릭 → GPT가 Boolean 조건식 자동 생성·표시 (사용자 검토·수정 가능)
4. **검색 실행** 클릭 → 리멤버 자동 검색·후보자 순회 시작
5. 실시간 진행 명세 표시: `12/30 처리 중 · 매칭 통과 5명 · 발송 완료 3명`
6. 각 후보자에 대해 GPT 매칭 점수 ≥ 임계값이면 즉시 자동 발송
7. 모든 후보자 처리 후 종료. 결과 리포트(검색·매칭·발송 통계) 표시

### 3.2 검증 시나리오 (테스트모드 ON, 클라이언트가 토글)
- 3-1과 동일하나 6단계가 다름:
  - 매칭 통과 후보자가 화면에 체크박스 리스트로 누적
  - 클라이언트가 검토·선별 → "선택 발송" 클릭 → 일괄 발송
- 매칭 품질 검수·신뢰 구축에 사용. 초기 며칠 운용 시 권장.

### 3.3 이상 감지 시나리오
- 로그아웃·차단 페이지·연속 발송 실패 3건 감지 시
  → 크롤러 즉시 정지 → UI에 사유 표시 → 사용자 수동 재개 필요

---

## 4. 기능 요구사항

### F-1. JD 입력
- 단일 큰 텍스트 영역. 형식 자유 (JD 본문, 회사 정보 포함).
- 제안 문구 템플릿 입력란은 사용하지 않음. 제안은 리멤버 사이트의 제안 버튼으로 자동발송.
- 통과 기준 슬라이더 (0–100, 기본 90).
- 테스트모드 토글 (기본 OFF, 마지막 값 기억).
- "조건식 생성" 버튼.

### F-2. Boolean 조건식 생성 (GPT)
- 입력: JD 텍스트
- 출력: 리멤버 검색창에 그대로 붙여넣을 수 있는 Boolean 조건식
- 사용자 수정 가능 (편집 가능 텍스트 영역으로 표시)
- "검색 실행" 버튼으로 다음 단계 진입

### F-3. 리멤버 자동 검색·순회 (Playwright)
- 조건식을 리멤버 검색창에 입력하고 검색 실행
- 검색 결과 리스트 페이징 처리
- 각 후보자 카드 순차 클릭 → 상세 페이지 진입
- 이력서 영역 텍스트 추출
- 후보자 간 30~90초 랜덤 지연

### F-4. 매칭 점수 산출 (GPT)
- 입력: JD + 이력서 텍스트
- 처리:
  1. GPT가 JD에서 핵심 평가 항목 추출 (직무경험, 기술스택, 연차, 지역 등)
  2. 각 항목별 0–100 채점
  3. 가중평균으로 총점 산출
- 출력: 총점 + 항목별 점수표 + 통과/탈락 사유(1~2문장)
- 항목 가중치: 기본 균등 가중, GPT 추천값 사용 (PRD 단계에서 더 정교화 안 함)

### F-5. 자동/반자동 발송
- 테스트모드 OFF:
  - 점수 ≥ 임계값 → 리멤버 후보자 상세의 제안 버튼 클릭 → 자동 발송
  - 발송 결과(성공/실패) 기록·표시
- 테스트모드 ON:
  - 점수 ≥ 임계값 → 체크박스 리스트에 추가만, 발송 안 함
  - "선택 발송" 클릭 시 선택된 후보자 일괄 발송 (사이 30~90초 지연)

### F-6. 실시간 진행 표시
- 진행 바: `N/M 처리 중`
- 현재 처리 중인 후보자 이름·직무
- 통계: 검색 결과 수 / 매칭 통과 수 / 발송 완료 수 / 발송 실패 수
- 갱신 방식: WebSocket 우선, 폴백으로 폴링 (구현 단계에서 확정)

### F-7. 이상 감지·자동 정지
- 트리거 (모두 활성):
  1. 로그아웃/세션 만료 감지
  2. 측하/차단 페이지 감지 (이상 활동 경고·캡차)
  3. 연속 발송 실패 3건
- 동작: 크롤러 즉시 종료 + UI에 사유 표시 + 로그 기록
- 재개: 사용자 수동 클릭

### F-8. 결과 리포트
- 실행 종료 시 요약: 검색 N건 · 매칭 통과 M건 · 발송 완료 K건 · 실패 L건
- 발송 실패 사유별 분류
- 다운로드/저장 기능은 MVP 제외 (화면 표시만)

---

## 5. 비기능 요구사항

### NF-1. 안정성
- 발송 간 30~90초 랜덤 지연 (기본값, .env로 조정 가능)
- 이상 감지 자동 정지 (F-7)
- 리멤버 계정 1개 제약 → 보수적 운용 우선
- ⚠️ 테스트모드 OFF + 일일 무제한 + 24h 운영 조합의 리스크 인지.
  첫 운용 1~2주는 사용자 모니터링 권장 (운영 가이드에 명시).

### NF-2. 보안
- API 키·로그인 정보 `.env` 분리, `.gitignore` 포함
- 웹 서버 `127.0.0.1` 바인딩만 (외부 노출 금지)
- 후보자 이력서 원문은 GPT 호출 외에는 로컬 저장 안 함
- 매칭 사유·총점만 진행 화면에 표시 후, 실행 종료 시 메모리에서 해제

### NF-3. 성능
- 후보자 1명 처리 시간: 검색·열람·GPT 매칭·발송 포함 평균 60~90초 (지연 포함)
- GPT API 응답 타임아웃 30초, 실패 시 1회 재시도

### NF-4. 가용성
- 단일 사용자 도구 → SLA 정의 없음
- 충돌 시 마지막 실행 상태 로그로 복구 단서 제공

### NF-5. 인수인계
- README + 설치 스크립트
- OpenAI API 키 발급 가이드
- 운영 가이드: 테스트모드 토글 방법, 이상 감지 발생 시 대처, 로그 위치

---

## 6. 화면 흐름

```
[1] 시작 화면
 ├ JD 텍스트 영역
 ├ 통과 기준/실행 범위 설정 영역
 ├ 통과 기준 슬라이더 (0~100, 기본 90)
 ├ 테스트모드 토글 (기본 OFF)
 └ [조건식 생성] 버튼
              ↓
[2] 조건식 확인 화면
 ├ GPT 생성 Boolean 조건식 (편집 가능)
 ├ JD 요약 (GPT 추출 항목·가중치 미리보기)
 └ [← 뒤로] [검색 실행 →] 버튼
              ↓
[3] 진행 화면
 ├ 진행 바 + N/M 처리 중
 ├ 현재 후보자 정보
 ├ 통계 (검색·매칭·발송·실패)
 ├ 실시간 로그 스트림
 ├ (테스트모드 ON) 매칭 통과자 체크박스 리스트 + [선택 발송]
 └ [일시정지] [중단] 버튼
              ↓
[4] 결과 화면
 ├ 실행 요약 통계
 ├ 발송 실패 사유별 분류
 └ [새 실행] 버튼
```

UI는 단일 페이지 앱 구조로, 위 4단계가 같은 페이지에서 섹션 전환으로 표현됨.

---

## 7. 기술 스택

| 영역 | 선택 | 비고 |
| --- | --- | --- |
| 언어 | Python 3.11+ | |
| 브라우저 자동화 | Playwright | 현장 검증 통과 시 고정. 차단 시 Selenium 폴백 검토 |
| 웹 백엔드 | FastAPI | WebSocket 내장, 비동기 친화적 |
| 웹 프론트 | 바닐라 JS + HTML + CSS | 별도 빌드 없이 정적파일 서빙 |
| 실시간 통신 | WebSocket (우선) / 폴링 (폴백) | |
| LLM | OpenAI API (모델은 비용·정확도 봐서 결정, 잠정 `gpt-4o-mini`) | API 키: 클라이언트 |
| 비밀정보 | `.env` (`python-dotenv`) | |
| 패키지 관리 | `pip` + `requirements.txt` | 회사 PC 환경 단순화 우선 |
| 로깅 | 표준 `logging` + 회전 파일 핸들러 | 한국어 메시지 |

---

## 8. 데이터 처리·개인정보

- 후보자 이력서 텍스트는 OpenAI API로 전송됨 (불가피).
  → 클라이언트 운영 가이드에 명시, 사전 동의 확보.
- GPT 호출 시 필요한 텍스트(이력서 본문·JD)만 전송. 식별성 높은 항목
  (이름·연락처)은 매칭 판단에 불필요하므로 마스킹 후 전송 검토.
- 로컬에는 원문 저장 안 함. 메모리 내에서만 처리.
- 로그에는 후보자 식별값(예: 리멤버 내부 ID) + 매칭 결과만 기록.
  이력서 원문·연락처 로그 금지.

---

## 9. 가정·리스크

### 9.1 핵심 가정 (현장 검증으로 풀어야 함)
- (A1) VPN 환경에서 OpenAI API 외부 호출이 차단되지 않음
- (A2) VPN 환경에서 Playwright 브라우저 자동화가 차단되지 않음
- (A3) 리멤버 DOM 구조가 자동화 가능한 형태 (SPA로 동적 렌더링은 가능하나 셀렉터 안정성 확보 가능)
- (A4) 리멤버 봇 탐지가 30~90초 지연 + 인간적 패턴으로 회피 가능

### 9.2 리스크
| 리스크 | 영향 | 완화책 |
| --- | --- | --- |
| 리멤버 계정 정지 | 클라이언트 업무 마비 | 보수적 지연, 이상 감지 자동 정지, 초기 모니터링 |
| VPN 정책 변경으로 자동화 차단 | 프로그램 미동작 | 현장 검증 조기 수행, 검증 실패 시 환불 협의 |
| GPT 매칭 오판 → 부적합 후보 발송 | 클라이언트 평판 손실 | 테스트모드 권장, 매칭 사유 표시로 검증 가능성 확보 |
| OpenAI API 비용 폭증 | 클라이언트 비용 부담 | 모델 선택(가성비) + 일일 비용 알림 기능(v2) |
| 후보자 개인정보 외부 전송 | 컴플라이언스 이슈 | 동의 확보, 마스킹, 로컬 저장 금지 |

---

## 10. 인수·완료 기준

다음 7항목이 모두 충족되면 MVP 인수 완료로 본다.

- [ ] (AC-1) VPN PC에서 프로그램 설치·실행 가능
- [ ] (AC-2) JD 입력 → Boolean 조건식 자동 생성 동작
- [ ] (AC-3) 리멤버 자동 검색 → 후보자 5명 이상 순회 → 이력서 텍스트 추출 동작
- [ ] (AC-4) 매칭 점수 표시 (항목별 점수표·총점·사유)
- [ ] (AC-5) 테스트모드 ON에서 일괄 승인 발송 동작
- [ ] (AC-6) 테스트모드 OFF에서 자동 발송 동작 + 이상 감지 3종 작동 확인
- [ ] (AC-7) 인수인계 문서 (README, API 키 가이드, 운영 가이드) 전달

---

## 11. 일정

| 단계 | 산출물 | 메모 |
| --- | --- | --- |
| (지금) 요구사항 확정 | PROJECT_CONTEXT.md, PRD.md | 완료 |
| ① 현장 검증 | 검증 결과 PROJECT_CONTEXT.md 7번 반영 | 가정 A1~A4 |
| ② 핵심 흐름 구현 | JD→조건식→검색→매칭→테스트모드 발송 | 테스트모드 우선 |
| ③ 자동 발송·이상 감지 | 테스트모드 OFF 흐름 + F-7 | |
| ④ 인수인계 문서 | README, 운영 가이드 | |
| ⑤ 시연·인수 | 클라이언트 입회 시연 | AC-1~7 점검 |

납기는 계약상 "이번 주 내". 단 ① 단계에서 가정 A1~A4 검증 결과에 따라
지연 가능성 있음 (계약 시 사전 고지됨).

---

## 12. PRD 후속 작업 (Claude Code에서 이어서 할 일)

1. **현장 검증** (VPN PC 원격 접속)
   - `curl https://api.openai.com/v1/models` 등으로 외부 API 호출 가능 여부
   - `playwright install chromium` + 간단 페이지 로드 테스트
   - 리멤버 로그인 → 검색·상세·발송 페이지 DOM 캡처, 셀렉터 후보 정리
2. 검증 결과를 `PROJECT_CONTEXT.md` 7번 항목에 반영, 가정 A1~A4 해소
3. 검증 통과 시 ② 단계(핵심 흐름) 진입. 차단 발견 시 대안 협의 (Selenium 폴백,
   다른 자동화 방식, 또는 환불 협의)
4. 구현은 테스트모드 ON 흐름부터 → OFF 흐름 → 이상 감지 순으로 단계적 검증

---

## 부록 A. 6번 합의 사항 인덱스 (PROJECT_CONTEXT.md 참조)

| 번호 | 결정 |
| --- | --- |
| 6-1 | 우선순위: 빠른 완성 > 안정성 > 정확도 > 확장성 |
| 6-2 | 발송 범위: 테스트모드 토글 방식 |
| 6-3 | JD 입력: 통짜 텍스트, 저장 안 함 |
| 6-4 | UI: 로컬 웹 + 실시간 진행 명세 |
| 6-5 | 매칭 산출: 항목별 점수 + 가중평균 |
| 6-6 | 통과 기준: UI 슬라이더(기본 90) |
| 6-7 | 제안 발송: 리멤버 사이트 기본 제안 버튼 자동발송 |
| 6-8 | 종료 정책: 미달이어도 종료 (자동 조건 완화 없음) |
| 6-9 | 테스트모드 기본값: OFF |
| 6-10 | API 키: 클라이언트 본인 키 |
| 6-11 | 안전장치: 무제한·30-90s 랜덤·이상감지 3종·24h |
