# 헤드헌팅 후보자 제안 자동화 — 프로젝트 컨텍스트

> 이 문서는 크몽 클라이언트 의뢰 건을 Claude Code에서 이어서 진행하기 위한
> 핸드오프 문서입니다. PRD 작성 전 단계의 합의 내용과 미확정 항목을 담고 있습니다.

---

## 1. 프로젝트 개요

- **의뢰 경로**: 크몽 (https://kmong.com/gig/756316), 99,000원, 결제 완료 (주문번호 7361793)
- **목적**: 헤드헌터가 반복 수행하는 후보자 검색·제안 업무를 자동화
- **범위**: 복잡한 CRM 불필요. "사람이 반복하는 작업의 자동화"가 핵심
- **납기**: 이번 주 내 목표 (리멤버 보안 이슈 시 지연 가능 — 클라이언트에 사전 고지됨)
- **개발자 일정 제약**: 병원 일정으로 일부 날짜 집중 어려움 (클라이언트 양해됨)

## 2. 클라이언트의 현재 업무 흐름 (As-Is)

1. ChatGPT로 Boolean 검색 조건식 생성
2. 리멤버 검색창에 조건식 입력
3. 검색된 후보자 순차 클릭
4. 이력서 텍스트 확인
5. ChatGPT로 JD 매칭도 분석
6. 매칭도 90% 이상이면 제안 발송
7. 목표 수량까지 반복

## 3. 원하는 기능 (To-Be)

- JD 입력 → ChatGPT API 기반 Boolean 조건식 생성
- 리멤버 자동 탐색: 조건식 입력 및 검색
- 후보자 순차 클릭 → 이력서 열람 및 텍스트 추출
- AI 매칭 분석: JD + 이력서 기반 매칭도 산출, 90% 이상 통과 처리
- 자동 제안 발송: 리멤버 사이트의 기본 제안 버튼 자동 클릭
- 반복 실행: 목표 수량 설정, 미달 시 새 조건식 생성 후 반복, 달성 시 종료

## 4. 사용 환경

- Windows PC, 1인 사용
- 리멤버는 특정 회사 PC에서만 로그인 가능 (후보자 개인정보 보안 정책)
- 해당 회사 PC는 SSL VPN 환경 (BLUEMAX ESP SSL VPN)
- 클라이언트는 회사 PC(VPN)에 개인 PC로 원격 접속하여 사용
- **개발 방식 합의**: 개발자가 구글 Chrome 원격 데스크톱으로 회사 PC에 접속하여
  해당 PC 내부에 직접 개발·설치 (별도 원격 PC 화면제어 방식 아님)

## 5. 개발자가 클라이언트에게 답변한 내용 (계약 시 약속)

- VPN 환경에서 자동화 가능하다고 답변
- OpenAI API 호출 구현 가능하다고 답변
- 프로그램은 VPN PC에 직접 설치, 원격으로 실행 권장
- 크롤링은 문제 없을 것으로 판단 (단, 리멤버 보안 이슈는 리스크로 남겨둠)
- 실패 시 보수 환불 약속

## 6. 합의된 결정사항 (Claude와의 사전 논의 결과)

### 6-1. 우선순위 (높은 순)
1. **빠른 완성** — 이번 주 내 납품
2. **안정성** — 차단/계정정지 리스크 최소화
3. **정확도** — 매칭 품질
4. **확장성** — 향후 SaaS 가능성

> 주의: 1순위(빠른 완성)와 2순위(안정성)는 부분 충돌함.
> 합의된 해석: "빠른 완성" = '완전 무인 자동 시스템'이 아니라
> '이번 주 내 실제로 반복 업무를 줄여주는 작동하는 도구 납품'.
> 발송 직전 사람 확인 같은 안전장치는 '미완성'이 아니라 '의도된 설계'로 본다.

### 6-2. 자동 제안 발송 범위 ✅ 확정
- **테스트모드 토글 방식 채택**:
  - **테스트모드 ON**: 매칭 통과 후보자를 리스트(체크박스)로 보여줌 →
    클라이언트가 검토·체크 → '선택 발송' 버튼 클릭 시 일괄 발송.
  - **테스트모드 OFF**: GPT 매칭도 통과 시 자동 발송 (안전장치 적용).
- 이유: 초기에는 매칭 품질을 직접 검수하며 신뢰를 쌓고, 검증된 이후엔
  무인 자동으로 효율을 끌어올리는 단계적 운용 가능.
- **안전장치(완전 자동 모드 공통)**:
  - 일일 발송 상한 (수치 미확정 → 7번 항목)
  - 발송 간 랜덤 지연 (수치 미확정 → 7번 항목)
  - 이상 패턴/오류 감지 시 즉시 정지
- 단, 테스트모드 기본값과 안전장치 수치는 7번에서 확정.

### 6-3. JD 입력 형태 ✅ 확정
- **통짜 텍스트 붙여넣기**: 큰 텍스트 영역 1개에 채용 공고/회사 정보를 통째로
  붙여넣음 → GPT가 파싱하여 Boolean 조건식·매칭 기준 추출.
- 항목별 폼·메타 필드 입력 없음 (GPT 파싱 신뢰).
- **저장/재사용 없음**: 실행할 때마다 새로 입력. JD 히스토리·발송 이력 DB는
  MVP 범위 외. 추후 필요해지면 확장.

### 6-4. 프로그램 UI 형태 ✅ 확정
- **로컬 웹 UI 채택**: VPN PC 내부에서 Python 백엔드 구동 → 브라우저로
  localhost 접속. 외부 노출 없음(127.0.0.1 바인딩).
- 데스크톱 앱 패키징/배포 번거로움 및 VPN PC 권한 이슈 회피.
- 후보자 리스트·매칭 결과·승인 버튼 표시에 웹이 가장 적합.
- **진행 상황 표시**: 실시간 진행 명세 + 핵심 지표
  (진행 바 'N/M 처리 중', 현재 검색어, 매칭 통과 수, 발송 완료 수 실시간 갱신).
  구현 방식(WebSocket vs 폴링)은 PRD에서 확정.

### 6-5. 매칭 산출 방식 ✅ 확정
- **항목별 점수 + 가중평균** 방식.
- JD에서 GPT가 핵심 항목(직무경험, 기술스택, 연차, 지역 등)을 추출 →
  이력서 텍스트와 비교하여 항목별 0–100 채점 → 가중치 적용 평균.
- 항목 가중치는 JD 성격에 따라 GPT가 추천하되, 기본 균등 가중에서 출발.
- 결과 표시: 총점 + 항목별 점수표 + 통과/탈락 사유(짧은 문장).
- 이유: 검증·재현성·근거 명확성. 같은 JD 반복 실행 시 결과 흔들림 최소화.

### 6-6. 매칭 통과 기준치 ✅ 확정
- **UI 슬라이더로 조절 가능** (0–100, 기본값 90).
- 실행 시작 화면에서 JD 입력란 옆/아래에 슬라이더 배치.
- 명세/특수 JD는 임계값을 낮춰 후보자 풀을 확보하는 운용 가능.
- 슬라이더 값은 해당 실행 1회에만 적용 (저장 안 함, JD와 함께 휘발).

### 6-7. 제안 발송 방식 ✅ 변경
- 별도 제안 문구 템플릿은 사용하지 않음.
- 리멤버 사이트에서 제공하는 제안 버튼의 기본 자동발송 흐름을 사용.
- 앱은 매칭 통과 후보자를 선별하고 제안 버튼 클릭까지 자동화.

### 6-8. 목표 수량 미달 시 동작 ✅ 확정
- **종료**: 한 번 입력된 조건식으로 모든 후보자를 처리한 뒤 미달이어도
  그대로 종료. '몇 명 발송 완료/미달 N명' 보고만 제공.
- **클라이언트 원본 요청과의 차이 명시**: 의뢰서의 "목표 미달 시 새 조건식 생성
  후 반복" 기능은 MVP 범위에서 **제외**. 이유:
  - 자동 조건 완화는 매칭 품질 저하 → 부적합 후보자 발송 위험.
  - 같은 계정으로 광범위한 반복 검색은 리멤버 봇 탐지 패턴에 가까움.
  - 빠른 완성(1순위)을 위해 MVP 범위 축소가 합리적.
- 향후 운용해보고 필요 시 v2에서 '사용자 확인 후 GPT 완화 1회' 추가 검토.

### 6-9. 테스트모드 기본값 ✅ 확정
- **기본 OFF** (효율 우선). 첫 실행부터 자동 발송 가능.
- UI 우측 상단(또는 실행 시작 화면)에 "테스트모드 ON/OFF" 토글 상시 노출.
- 상태는 마지막 값 기억(로컬 설정 파일 저장).
- **함의**: 첫 실행부터 무인 발송 가능 → 6-10 안전장치 수치가 더 중요해짐.
  특히 일일 발송 상한·이상 감지 트리거는 보수적 기본값 권장.

### 6-10. OpenAI API 키 주체 ✅ 확정
- **클라이언트 본인 키 사용**. 클라이언트가 OpenAI 계정 생성·결제수단 등록 후
  API 키 발급 → 프로그램 설정(.env)에 입력.
- 사용량·비용은 클라이언트가 OpenAI 콘솔에서 직접 확인.
- **인수인계 항목**: API 키 발급 절차 가이드 동봉 필요.
  (개발자 인계 시 일회성 작업)
- 비용 예측치(잠정): GPT-4o-mini 기준 후보자 1명 매칭 약 $0.001~0.005 수준,
  하루 100명 처리 시 월 $3~15. 정확한 모델 선정은 PRD에서 확정.

### 6-11. 자동 발송 안전장치 ✅ 확정 (잠정, 현장 검증 후 재조정)
- **일일 발송 상한**: 무제한.
  단, 발송 간격(아래)이 자연 상한 역할 — 24h 풀가동 시 이론상 최대 ~1,440건,
  실제로는 매칭 통과 후보자 수에 자연 제한됨.
  → 현장 검증 후 첫 운용 1~2주 모니터링하며 위험 시 즉시 상한 도입 권장.
- **발송 간 지연**: 30~90초 랜덤. 평균 ~60초, 시간당 약 60건.
- **이상 감지 자동 정지 트리거** (모두 활성):
  1. 로그아웃/세션 만료 감지 (로그인 페이지 리다이렉트)
  2. 측하/차단 페이지 감지 (이상 활동 경고·캡차 등장)
  3. 연속 발송 실패 3건 (성공 메시지 미수신, UI 변경 가능성)
- **비활성 트리거**: 발송 거부율 임계 (장기 운용 시 추후 검토).
- **공통 동작**: 정지 시 즉시 크롤러 종료 + UI에 사유 표시 + 로그 기록.
  재개는 사용자 수동 클릭 필요.
- **운영 시간대**: 제한 없음 (24h). 사용자가 명시적 종료 시까지 동작.
  ⚠️ 위험 누적 인지: 테스트모드 OFF + 일일 무제한 + 24h 운영의 조합은
  리멤버 이상 패턴 탐지에 가장 노출되는 설정. 이상 감지 트리거 3종이
  유일한 자동 방어선이므로 첫 운용 1~2주는 사용자 직접 모니터링 권장.
  운영 결과에 따라 시간대/상한 도입 여부 재검토.

## 7. 미확정 항목 (PRD 작성 전 해소 필요)

- [ ] **리멤버 사이트 실제 구조**: 검색 결과 페이지, 후보자 상세, 제안 발송 UI의
      DOM 구조. → 개발자가 원격 접속 후 확인 예정. 확인 전까지 셀렉터는 미확정.
- [ ] **VPN/보안 정책의 브라우저 자동화 차단 여부**: Selenium/Playwright 실행 가능
      여부. 클라이언트도 "제한 있을 것 같다"고 언급. 실제 PC에서 검증 필요.
- [x] **매칭도 산출 방식**: 항목별 점수 + 가중평균으로 확정 (6-5 참조).
- [x] **매칭 통과 기준치**: UI 슬라이더 조절(기본 90)로 확정 (6-6 참조).
- [ ] **크롤링 안정성**: 요청 속도 제한, 세션 유지, 캡차/봇 탐지 대응, 에러 복구.
- [x] **자동 발송 범위 최종 확정**: 테스트모드 토글 방식으로 확정 (6-2 참조).
- [x] **테스트모드 기본값**: OFF로 확정 (6-9 참조).
- [x] **자동 발송 안전장치 수치**: 6-11 참조 (현장 검증 후 재조정 여지).
- [x] **운영 시간대 제한**: 24h 무제한으로 확정 (6-11 참조).
- [x] **OpenAI API 키 주체**: 클라이언트 본인 키로 확정 (6-10 참조).
- [x] **제안 발송 방식**: 별도 문구 템플릿 없이 리멤버 제안 버튼 자동발송으로 변경 (6-7 참조).
- [x] **목표 수량 미달 시 동작**: 종료(반복 없음)로 확정 (6-8 참조).
      ※ 의뢰서의 '자동 새 조건식 생성' 기능은 MVP에서 제외, v2 검토.

## 8. 알려진 리스크

- 리멤버는 후보자 개인정보를 다루는 사이트 → 봇 탐지·자동화 차단 정책이
  강할 가능성. 계약서상 "실패 시 환불"이므로 기술 검증을 조기에 해야 함.
- VPN(BLUEMAX ESP SSL VPN) 환경에서 OpenAI API 외부 호출이 막힐 가능성 →
  최우선으로 PC 접속하여 ping/curl 테스트 필요.
- 계정 1개로 운영 → 정지 시 클라이언트 업무 전체 마비. 보수적 속도 설정 필수.
- 개인정보 취급: 후보자 이력서 텍스트를 OpenAI API로 전송 → 데이터 처리
  관련 클라이언트 동의 및 최소 전송 원칙 명시 필요.

## 9. 다음 단계 (Claude Code에서 이어서 할 일)

- [x] **PRD 작성** → `PRD.md` 완료 (2026-05-26)
- [ ] **현장 검증** (VPN PC 원격 접속):
  - OpenAI API 외부 호출 가능 여부 (curl 테스트)
  - Playwright 설치 및 브라우저 구동 가능 여부
  - 리멤버 로그인 후 검색/상세/발송 페이지 DOM 구조 캡처
- [ ] 검증 결과를 본 문서 7번 항목에 반영, PRD 9.1 가정(A1~A4) 해소
- [ ] MVP 구현 (PRD 11. 일정 ②~⑤ 단계 진행):
  1. 테스트모드 ON 흐름 (JD→조건식→검색→매칭→일괄승인 발송)
  2. 테스트모드 OFF 흐름 (자동 발송)
  3. 이상 감지 트리거 3종
  4. 인수인계 문서
  5. 클라이언트 입회 시연 (AC-1~7 점검)
